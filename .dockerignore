# Build artifacts (MUST INCLUDE FOR DOCKER BUILD)
# Note: We need Cargo.lock for reproducible builds
# Note: We need target/ directories to be empty for clean builds

# Development environment files
.env
.env.local
.env.production
*.log
leptos-server.log
monitor.log

# Git files
.git/
.gitignore
.gitattributes

# Documentation (keep only essential docs)
*.md
!README.md
docs/

# IDE and editor files
.vscode/
.idea/
.claude/
.agent/
*.swp
*.swo
*~

# Build artifacts (keep Cargo.lock for reproducible builds)
# Use ** glob to match nested target directories
**/target/
**/target
target/
target
.cargo/
**/.cargo/

# SQLx offline data
.sqlx/

# Reference folder (large, not needed for build)
reference/
reference

# CI/CD (GitHub Actions runs outside Docker)
.github/

# Node modules (if any)
node_modules/
node_modules
yarn.lock

# Testing artifacts
coverage/
.coverage
htmlcov/
playwright-report/
test-results/

# Temporary files
tmp/
temp/
errors.txt

# OS-specific files
.DS_Store
Thumbs.db
desktop.ini

# Backup files
*.bak
*.backup
*.old
*.orig
