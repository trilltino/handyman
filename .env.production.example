# Production Environment Variables for Handyman Marketplace
#
# IMPORTANT: Copy this file to .env.production and fill in real values
# NEVER commit .env.production to version control

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL password (used in docker-compose)
POSTGRES_PASSWORD=REPLACE_WITH_SECURE_PASSWORD_MIN_32_CHARS

# Database URL (automatically constructed in docker-compose)
# DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/handyman

# =============================================================================
# JWT Authentication
# =============================================================================
# Generate with: openssl rand -base64 64
JWT_SECRET=REPLACE_WITH_SECURE_SECRET_MIN_64_CHARS

# Token expiration (in hours, optional, default: 24)
TOKEN_DURATION_HOURS=720

# =============================================================================
# Stripe Configuration
# =============================================================================
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_REPLACE_WITH_LIVE_KEY

# Get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_WITH_WEBHOOK_SECRET

# =============================================================================
# =============================================================================

# =============================================================================
# Server Configuration
# =============================================================================
# Server port (optional, default: 3000)
PORT=3000

# Log level
RUST_LOG=info
